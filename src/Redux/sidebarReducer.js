let initialState = {
    sidebar : [
        {id: 1, name: 'Dasha'},
        {id: 2, name: 'Pasha'},
        {id: 3, name: 'Karina'}
    ]
}

const sidebarReducer = (state = initialState, action) => {
    // break
    return state
}

export default sidebarReducer